interface:
  display_name: ad-handoff
  short_description: Compact the session into a temporary handoff whose resume protocol composes the canonical decision-maker brief while retaining rules, roadmap, live state, lost asks, hygiene, and redaction.
policy:
  allow_implicit_invocation: true
