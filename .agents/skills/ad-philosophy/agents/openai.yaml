interface:
  display_name: ad-philosophy
  short_description: Load universal agent guardrails (think, ground, decide when grounded, simplify, surgical, goal-driven, verify, report for a decision-maker) on non-trivial work.
policy:
  allow_implicit_invocation: true
