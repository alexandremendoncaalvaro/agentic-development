# Task 0001: Apply kit to itself — generate AGENTS.md and ARCHITECTURE.md for this repo

**Status:** done
**Created:** 2026-05-08
**Owner:** Alexandre Alvaro
**Board ref:**

## Context

Before writing any v0.2 code, the kit must prove it can produce its own operational artifacts. This is Step B of [doc/v0.2-cli-plan.md](../v0.2-cli-plan.md): apply the existing prompts manually to this repository, validate the templates work in practice, and produce real `AGENTS.md` and `ARCHITECTURE.md` files that will guide future agentic work on the kit itself.

## Acceptance Criteria

- [x] `AGENTS.md` exists at the repo root, generated by feeding the current repo state to [`prompts/agents.md`](../../prompts/agents.md) in brownfield mode <!-- 86 lines, hand-cut in Task 0006 after the original 199-line bootstrap output exposed Bugs A and B -->
- [x] `ARCHITECTURE.md` exists at the repo root, describes the current CLI structure (commands, lib modules, kit layout) <!-- 68 lines, written 2026-05-09 by running prompts/architecture.md scan-first -->
- [x] Both files reflect actual reality: every section either has real content or an explicit `<TODO>` marker — no invented values
- [x] Both files are consistent with [`doc/v0.2-cli-plan.md`](../v0.2-cli-plan.md) (no contradictions about scope, terminology, or architecture)

## Plan

- [x] Open Claude Code on this repo with kit access (`claude --add-dir .` since the kit is this repo for now)
- [x] Paste [`prompts/agents.md`](../../prompts/agents.md) and run the brownfield interview <!-- pre-Task 0006 run produced the 199-line bloated draft; Task 0006 hand-cut to 86 lines -->
- [x] Review the generated `AGENTS.md`, refine, commit on `cli` branch
- [x] Paste [`prompts/architecture.md`](../../prompts/architecture.md) and run the interview <!-- 2026-05-09 session, scan-first variant per Task 0006 rewrite -->
- [x] Review `ARCHITECTURE.md`, refine, commit on `cli` branch
- [x] Cross-check both files against [`doc/v0.2-cli-plan.md`](../v0.2-cli-plan.md) for consistency
- [x] If any template or prompt felt awkward during the interview, log it in Notes — refine the templates before v0.2 code starts <!-- captured in Task 0006 — Bugs A/B and the cross-doc audit are the refinements -->


## Notes

### 2026-05-08

First dogfood pass produced an `AGENTS.md` of 199 lines with two defects: (1) the bootstrap interview asked questions whose answers were already in the repo or were architectural decisions, not user input; (2) the output bundled 82 lines of generic agent advice (the documented #1 anti-pattern per agents.md / GitHub analysis). Treated as a bug — followup [Task 0006](0006-bootstrap-flow-and-agents-md-bloat-fix.md) and ADR-0005 cover the fix and the architectural decision (Universal Agent Behavior moves to a skill, not inline). After Task 0006 lands, re-run this task's first checkbox to confirm the new flow produces an `AGENTS.md` that meets the kit's own quality bar.

### 2026-05-09 — close-out: ARCHITECTURE.md generated, AGENTS.md verified post-Task 0006

`AGENTS.md` (86 lines, hand-cut in Task 0006) covers AC #1. This session generated `ARCHITECTURE.md` (68 lines) by running [`prompts/architecture.md`](../../prompts/architecture.md) scan-first against the repo state (commits up to `9670408`), covering AC #2.

AC #3 — `<TODO>` markers used only where signal genuinely missing. `Async/messaging` and `Metrics/Traces` documented as N/A given the CLI shape (single-process, short-lived); no fabricated content.

AC #4 — cross-checked against [`doc/v0.2-cli-plan.md`](../v0.2-cli-plan.md). No contradictions: the plan describes the v0.2 destination (Phase 1 CLI + Phase 2 skills); `ARCHITECTURE.md` describes the current state (Phase 1 only, no `src/skills/` yet) and names Tasks 0002/0003 as the owners of the skills layer. When skills ship, `ARCHITECTURE.md` gets a Phase 2 section then.

ADR candidates flagged in `ARCHITECTURE.md` (boundary rule for `KIT_ROOT` reads, stdout/stderr observable contract, mode-validation strategy via Commander `.choices()`) — none are binding yet, all have a written trigger condition. No ADR opened.

Empirical end-to-end dogfood of the brownfield bootstrap (running the prompt against a scratch copy and producing a fresh `AGENTS.md`) deliberately deferred — [`test/init-output.test.js`](../../test/init-output.test.js) already guards the prompt structure, and the meaningful next dogfood is the `agentic-bootstrap` skill once Task 0002 lands.

Test status: `npm test` green (`bin/agentic.js --help`, `init --help`, `test/init-output.test.js`). Status flipped to `done`. Picking up handoff at step 3 — Task 0002.

## Definition of Done

All Acceptance Criteria checked, plus:

- [x] Local tests pass (or N/A documented in Notes) — `npm test` green at close-out
- [x] Code review completed (human or fresh-context reviewer per WORKFLOW §10) <!-- author review during scan-first generation; fresh-context review can run before Task 0002 if desired -->
- [x] No orphan `TODO`/`FIXME` introduced
- [x] Status updated to `done` and Notes log closes the task
