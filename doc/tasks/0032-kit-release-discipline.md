# task-0032: Release discipline for the kit repo

**Status:** done
**Date:** 2026-08-05

## Goal

A `scripts/release.sh` + `CHANGELOG.md` + changelog commit gate for this repo, so releases stop being hand-assembled. Principle (observed in a sibling methodology repo and felt here on 2026-08-05: manual version bump, an over-length commit subject, a tag that needed a retry): **merged is not released in a package repo** — consumers install the published version, not `main`, so an unlogged or unreleased change is invisible to every install.

## Checklist

- [x] `CHANGELOG.md` with an `[Unreleased]` section; seed it retroactively from v0.19.0-beta.1's content (ADR-0046/0047).
- [x] `scripts/release.sh <patch|minor|major|prerelease>`: bumps `package.json` (+ lockfile), rotates `[Unreleased]` into the new version heading, commits (`chore(release): x.y.z`, DCO), creates the annotated tag — and deliberately does NOT push or publish. `--dry-run` wraps every destructive op.
- [x] Changelog gate in `lefthook.yml` pre-commit: a commit touching `src/skills/`, `src/lib/`, `src/commands/`, or `bin/` without an `[Unreleased]` entry warns (advisory-first — see ad-hooks' debut rule); flip-to-block criterion: two weeks without a false positive.
- [x] Subject-length check (≤72, imperative) in the same gate — the exact slip that happened on 2026-08-05.
- [x] Document the publish tail in README: `npm publish` (2FA prompt) + dist-tag check — `latest` did not follow `publishConfig.tag: beta` on 0.19.0-beta.1 and needed a manual `dist-tag add`.
- [x] Tests: bats-style or node:test coverage for the rotate/bump logic; `npm test` green.
- [x] Self-audit with `/ad-audit` before PR.

## Notes

- 2026-08-05 — Registered from the SLIM/NEST mechanism investigation. The sibling repo's version also bundles a protected-branch gate; this repo's "never push directly to main" is already convention — evaluate whether a mechanical guard earns its cost during implementation.
- 2026-08-05 — Implemented on `chore/kit-release-discipline`. Pure logic (`bumpVersion`, `rotateChangelog`) TDD'd in `scripts/release-lib.js` + `test/release.test.js`; gates follow the leak-guard shape (pure policy core + thin git wrapper) in `scripts/changelog-gate.js` and `scripts/commit-subject-check.js` + `test/commit-gates.test.js`. Subject length blocks (deterministic per ad-hooks' debut rule); the changelog pairing and mood heuristics warn only. End-to-end verified on a throwaway branch: real run produced the signed `chore(release): 0.19.0-beta.2` commit, lockfile bump, and annotated tag carrying the changelog section (`--cleanup=whitespace`, or git eats the `###` headings as comments); guards observed refusing main (with the computed branch suggestion), an existing tag, a dirty tree, and an empty `[Unreleased]`. 345/345 tests green.
- 2026-08-05 — Protected-branch gate evaluation (the open question above): NOT absorbed as a standing hook. `release.sh` refusing to run on `main` is the mechanical guard scoped to where the incident lived; a general pre-push branch block duplicates GitHub-side protection and the repo convention without new coverage.
- 2026-08-05 — CORRECTION to the note above, from the `/ad-audit` self-audit: the "duplicates GitHub-side protection" premise was asserted without checking and is false — `gh api repos/alexandremendoncaalvaro/agentic-development/branches/main --jq '{protected}'` → `{"protected": false}`, and `.../rulesets` → `[]` (observed live during the audit). With no origin-side protection, the no-direct-push convention had zero mechanical enforcement, so the evaluation flips: `scripts/push-branch-guard.js` now blocks pushes updating `main`/`cli` at pre-push (deterministic, day-one block per ad-hooks' debut rule; verified against a local bare remote — push of `main:main` refused via the stdin ref lines, feature-branch push passed). Enabling GitHub branch protection on `main` remains recommended as the origin-side layer (owner action).
- 2026-08-05 — Sequencing correction, from the same audit: the "Self-audit with `/ad-audit`" box was ticked in commit `97dd913` (21:43 -0300) before the audit had run; the audit trail (`.agentic/reviews/2026-08-06T0046Z-audit-*.md`, eight reviewer handoffs) came 2m30s later. The box's claim is made true before the PR by this run: 6 group reviewers + 2 cross-model hardening passes on the CRITICAL CV group, findings unioned and every one either fixed on this branch or refuted with evidence; ADR-0048 records the gate/changelog decisions the audit flagged as unrecorded.
- 2026-08-05 — Evidence artifacts for the verification claims (all on branch head `97dd913` unless noted; re-run post-fix on the final head is recorded in the PR body):
  - `npm test` → `tests 345 / pass 345 / fail 0` at `97dd913`; `tests 356 / pass 356 / fail 0` after the audit fixes (new gate + regression tests).
  - Real `scripts/release.sh prerelease` on a scratch branch → commit `chore(release): 0.19.0-beta.2` with `Signed-off-by: Alexandre Alvaro <alexandre.alvaro@hotmail.com>`, `package.json` and `package-lock.json` both at `0.19.0-beta.2`, annotated tag body retaining `### Added` (requires `--cleanup=whitespace`; scratch branch and tag deleted after, now pinned permanently by `test/release-e2e.test.js`).
  - Guards observed refusing: dirty tree (`error: working tree has uncommitted changes`), existing tag (`error: tag v0.19.0-beta.2 already exists`), `main` (`error: refusing to commit a release on main` + computed `git switch -c chore/release-0.19.0-beta.2` suggestion), empty `[Unreleased]` (`error: [Unreleased] is empty — nothing to release`, no stack trace). All four are also pinned by `test/release-e2e.test.js`.
  - Hooks observed live: `changelog-gate` warned on a staged `src/` probe and exited 0; `subject-check` blocked an 83-char subject (exit 1) and passed a clean imperative subject (exit 0).
- 2026-08-06 — Merged: PR #66, merge commit a8a7936 (https://github.com/alexandremendoncaalvaro/agentic-development/commit/a8a7936dfa1ee7073c7f87317941251f2ce867f3). ADR-0048 ratified as accepted in the PR. Origin-side layer added post-merge: ruleset `protect-main` (PR required, `test (20.x)`/`test (22.x)` required, deletion and force-push blocked, no bypass).
- 2026-08-05 — Scope clarifications: the checklist's "in the same gate" for the subject check landed as its own `commit-msg` stage — the subject line only exists at commit-msg time, not pre-commit. The changelog gate's watched set widened from the checklist's four prefixes to everything `package.json#files` ships minus `README.md`/`LICENSE` — the audit showed `src/index.js`, `src/leak-guard.js`, `templates/`, `prompts/` and `WORKFLOW*.md` are also consumer-visible.
- 2026-08-06 — Provenance note: the commits this task shipped were authored and DCO-signed as the test-fixture identity `Audit Test <audit@example.com>` — the git-env leak class later diagnosed and fixed in task-0033 (a fixture `git config` write through hook-leaked `GIT_DIR` poisoned the shared repo config, and commits picked it up). Real authorship: Alexandre Alvaro; the DCO certification intent stands. History stays the immutable ledger; this note is the durable correction.
