# task-0033: Content anchor for ad-audit reviewers (anti-fabrication / anti-stale-read)

**Status:** done
**Date:** 2026-08-05

## Goal

A reviewer's claim that it audited the right artifacts becomes mechanically checkable: each `audit-group-reviewer` echoes a **content anchor** — the sha256 of the rules file(s) it read and the target SHA — and the aggregator cross-checks all reviewers audited the same rule-set version and tree. A "ran/read" assertion without a matching anchor is not trusted.

## Motivating incidents

- 2026-08-05 dogfood audit (ADR-0046): one CV second-pass reviewer reported its own Read tool serving stale pre-commit file content mid-review; it caught itself only by re-verifying against `git show HEAD:<path>`. An anchor check would surface this class mechanically.
- The pattern is proven in a sibling harness: its composite review gate honors a reviewer's `ran:true` only with a checkable anchor (skill version string or vendored-rubric content hash), added after a real incident of a reviewer asserting it ran when it hadn't.

## Checklist

- [x] Extend the ad-audit Step 1 deterministic probe to emit sha256 for each resolved rules file (machine store files + project layer files).
- [x] Handoff template: carry the expected anchors; reviewer brief (both hosts, .md + .toml): echo `Anchors: <file>=<sha256>, target=<SHA>` alongside `Files grounded`.
- [x] Aggregation (Step 6): anchor mismatch or absence ⇒ that reviewer's verdicts are UNVERIFIED (not silently accepted); re-dispatch or mark the group unaccounted.
- [x] ADR-0045 framing: this directs verification at an artifact class; it does not add lenses or pressure. State this in the ADR/commit.
- [x] Mirror on both hosts; dogfood `update --yes`; `npm test`; self-audit with `/ad-audit` before PR.

## Notes

- 2026-08-05 — Registered from the SLIM/NEST mechanism investigation. Interacts with task-0031 (preflight-scripts infrastructure): if 0031 lands first, the anchor emission belongs in the shipped probe script rather than the embedded snippet.
- 2026-08-06 — Implemented on branch `feat/audit-reviewer-content-anchor` (0031 landed first, so the anchors live in the shipped skill script). Probe prints `<file>=<sha256>` per machine-store/project-layer rule file, `<file>=UNREADABLE:<code>` for an unreadable one, binding docs bare (the target tree SHA pins them; the machine store lives outside the tree and project rules may be machine-local, so a content hash is what pins those). TDD observed: anchor tests written first, red run `node --test test/skill-scripts.test.js` → `pass 3 / fail 4` at `85876ad`+worktree, green after the emission landed → `pass 7 / fail 0`; the UNREADABLE regression test was likewise red (`pass 7 / fail 1`) before its fix, green after (`pass 8 / fail 0`). Full gate at head `8647099`: `npm test` → 372 tests, 370 pass, 0 fail, 2 skipped (pre-existing `lefthook binary not installed` skips, unrelated). Twins byte-identical: `shasum -a 256` on both hosts' `resolve-rules.mjs` → `baa5dc95352274fa017d90ca383bc586a0d4e0f94f840a9f1ad98fcc885fa31b`, and dogfood `node bin/agentic.js update --yes` left all four installed copies byte-identical to source (`diff` empty ×4, state files re-verified by reviewers at 92/92 `sourceSha` matches).
- 2026-08-06 — Self-audit record (`/ad-audit`): 7 group reviewers + 2 cross-model hardening passes on the CRITICAL CV group (opus in-host proxy, rules and hunks reordered between runs; trails `.agentic/reviews/2026-08-06T1757Z-audit33-*.md`, closure `...T1830Z-audit33-closure.md`) on target `07e7fd3`. First live use of the anchor mechanism: 9/9 reviewers echoed recomputed anchors, zero mismatches. The hardening passes surfaced one blocker (enforcement half without artifact) and three majors — all resolved at `719db8b` with observed evidence: EACCES probe-death regression reproduced (`chmod 000` fixture, exit 1, zero output) then fixed (`locked.md=UNREADABLE:EACCES`, exit 0) and test-pinned; EXPECTED ANCHORS now carry `(file: <absolute path>)` so an unaugmented handoff is resolvable; guard tests pin both contract carriers on both hosts (the SKILL.md guard caught a real codex gap — the `.toml` brief referenced an EXPECTED ANCHORS section the codex SKILL.md never named — red observed, then fixed; the brief guard was mutation-checked); negative-case artifact: a reviewer dispatched against a deliberately corrupted expectation (`...f45` vs actual `...f44`) recomputed, reported MISMATCH, and refused to proceed (trail `...T1819Z-audit33-anchor-negative.md`, target `8647099`). Pre-existing drifts surfaced (not this diff's): `.gitignore` carries `.agentic/rules/` where ADR-0043 mandates `.git/info/exclude`; AGENTS.md "Today: only init"; ADR-0035 resolution-in-instructions text vs the shipped script — queued for the /ad-drift sweep. Rule-gap candidate for /ad-level-up: GUIDELINES §2.2 exemption-or-logging for fs-existence-probe idioms.
- 2026-08-06 — Disclosures, per the audit. (a) Ships unmeasured (ADR-0046 Decision-3 disclosure pattern): the anchor echo is a standing reviewer-contract addition adopted without an arms-style measurement — it is mechanically decidable about the reviewer's own context and cannot inflate findings about the target (the inflation mechanism ADR-0045 measured); revisit trigger: if anchor checks start producing false UNVERIFIED verdicts (e.g. a rule file legitimately edited between probe run and dispatch), measure over a labelled range or downgrade the rule to advisory. (b) Honest ceiling, stated in both hosts' skill text: a matching echo is necessary, not sufficient — it cannot prove recomputation over copying; expectations persist in the trail for out-of-run re-checking; aggregator-held expectations are a recorded candidate refinement (deviates from this task's handoff contract; Alê's call). (c) The Motivating-incidents sibling-harness claim is recorded testimony from the registering investigation, not re-verified from this repo's toolset. (d) On this machine the dispatched reviewers ran the pre-change user-global brief (`~/.claude/agents/`, release-tracked) while the handoff carried the contract — which copy a host resolves is an OPEN QUESTION; the global install refreshes at the next release, never from an unmerged branch.
