interface:
  display_name: ad-rules
  short_description: Resolve global and project rules. Inventory mode lists topics; correction mode reapplies ad-philosophy, repairs the active plan, and returns the canonical decision-maker brief.
policy:
  allow_implicit_invocation: true
