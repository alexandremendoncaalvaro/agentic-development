interface:
  display_name: "Create an analytical report"
  short_description: "Draft, render, or review a verified analytical report"
  default_prompt: "Use $ad-report to turn these findings and evidence into a verified report."
policy:
  allow_implicit_invocation: true
