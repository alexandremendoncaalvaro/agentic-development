interface:
  display_name: ad-roadmap
  short_description: Reconcile project or task progress, compose the canonical decision-maker brief, then append a nested checklist with evidence-backed completion and blockers.
policy:
  allow_implicit_invocation: true
